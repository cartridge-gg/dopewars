import { Global } from "@emotion/react";

const GlobalStyles = () => (
  <Global
    styles={`
      #chakra-toast-manager-top-left {
        margin-top: 70px;
      }
    `}
  />
);

export default GlobalStyles;

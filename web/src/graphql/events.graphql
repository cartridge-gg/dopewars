query Event($id: ID!) {
  event(id: $id) {
    keys
    data
    createdAt
    systemCall {
      transactionHash
    }
  }
}

query Events($limit: Int) {
  events(limit: $limit) {
    keys
    data
    createdAt
    systemCall {
      transactionHash
    }
  }
}

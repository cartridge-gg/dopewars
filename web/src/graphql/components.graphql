query Component($id: ID!) {
  component(id: $id) {
    name
    classHash
    transactionHash
    createdAt
  }
}

query Components($limit: Int) {
  components(limit: $limit) {
    name
    classHash
    transactionHash
    createdAt
  }
}
